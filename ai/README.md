# AI subsystem boundaries

The folders below reserve the approved architectural boundaries. Phase 1 does not implement AI behavior. Each boundary is activated only in its assigned phase and must use typed, authorized interfaces rather than bypassing application services.
